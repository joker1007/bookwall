# Bookwall

電子書籍管理のためのServer及びReaderアプリケーションのプロジェクト。

## Server

### 実装言語

- Ruby on Railsの8.1系を使用してServerを実装する。
- gemは適宜調査して、実績のあるもの利用する。
- フロントエンドはTypeScriptとReactを使用して実装する。
- サーバーはFalconを利用し、非同期処理にはSolidQueueを利用する。

### データベース

- SQLiteを利用する。

### 機能

#### ライブラリ管理

- スキャン対象ディレクトリの登録、編集、削除
- 登録されたディレクトリのスキャン (マルチスレッドでのスキャンをサポート)
- スキャンされた電子書籍のメタデータの保存
  - ファイルパス
  - タイトル
  - シリーズ名
  - 巻数
  - 著者
  - タグ
  - 出版日
  - その他、将来的に追加の可能性アリ
- 電子書籍の検索機能(タイトル、著者、タグ、登録日)などで検索、ソートができる
- 電子書籍のメタデータの編集機能
- 電子書籍の削除機能 (ファイルは削除せず、メタデータのみ削除)
- 電子書籍のタグ管理機能 (タグの追加、削除、編集)
- お気に入り機能 (お気に入りの電子書籍の登録、削除、お気に入りの一覧表示)
- 表紙画像の生成 (CBZ、EPUB、PDFから表紙画像を生成)
  - 表紙画像はサムネイルとして使用するため、適切なサイズにリサイズして保存する
  - 保存形式はJPEGを基本とするが、将来的にWebPなどの形式もサポートする可能性アリ
- OPDSフィードの生成 (ライブラリ全体、ライブラリ毎、タグ別、お気に入りなどのOPDSフィードを生成)
  - OPDS-PSEをサポートする
- ユーザー登録とパスワードによる認証機能
  - 将来的に認証方式を追加する可能性アリ

#### サポートする電子書籍フォーマット

- CBZ
- EPUB
- PDF
- ディレクトリ直下の画像ファイル (JPEG, PNG, WebP)

CBZはComicInfo.xmlの読み込みをサポートし、EPUB、PDFはファイルに埋め込まれたメタデータをサポートする。
メタデータが存在しない場合は親ディレクトリ名をシリーズとして、ファイル名をタイトルとして扱う。

ディレクトリ直下の画像の場合は、ディレクトリ名をタイトルとして下位の画像ファイルをページとして扱う。
シリーズは必ずしも存在しない。

#### UI

- サイドバー
  - ホーム (最近追加されたシリーズの表示)
  - ライブラリはダイレクトにサイドバーから選択可能
  - すべてのシリーズ (シリーズを持たない書籍は表示しない)
  - 著者一覧
    - その著者の電子書籍の一番最初の書籍のサムネイルを表示
  - タグ一覧
  - お気に入り
- メイン画面
  - 表示形式の切り替え (グリッド表示、リスト表示)
      - リスト表示にもサムネイルは表示する
      - リスト表示時は、サムネイル、タイトル、著者、タグなどの情報を表示
      - グリッド表示時は、サムネイルとタイトルのみを表示
  - シリーズを表示する時は、シリーズ名と巻数を表示、サムネイルは最初の巻の表紙を表示
  - ソート機能 (タイトル順、シリーズ順、著者順、登録日順など)
  - フィルタリング機能 (タグ別、著者別など)
- トップに検索バー (タイトル、著者、タグなどで検索)
- トップバーに設定アイコンを配置し、クリックすると設定画面に遷移する
  - 設定画面では、スキャン対象ディレクトリの管理や、OPDSフィードのURLの確認などができるようにする

#### Web Reader

ブラウザ上で電子書籍を閲覧するための機能

- CBZ、EPUB、PDFの表示をサポートする
- ディレクトリ直下の画像ファイルの場合は、CBZと同様にページ送りで表示する
- ページ送りの操作は、キーボードの矢印キーやスペースキー、マウスのクリックなどで行えるようにする、ページ戻りはスペースの代わりにバックスペースを利用可能にする。
- 左から右に読むのか右から左に読むのかを選択できる様にする。
- EPUBの場合は、目次の表示や、フォントサイズの変更、文字スペースや行間の変更、背景色の変更などの機能もサポートする
  - 将来的にはフォントの登録・変更もサポートする
- ページジャンプのためのスクロールバーを下部に配置し、表示をトグルできるようにする
- スクロールバーを操作する時は対応するページのサムネイルを表示するようにする
- パフォーマンスのためにページのプリロードをサポートする
- サーバーサイドで余計なI/Oを発生させないように、ページのキャッシュ機能をサポートする
- CBZ、PDFの場合は見開き表示のために2ページ同時表示をサポートする。
- オフセットがズレている場合を想定し、見開き表示モードでも1ページずつページ送りができる様にする。
- リーダーの設定は書籍ごとに異なる可能性があるため、リーダーの設定を書籍ごとに保存できる様にする。
- EPUBは縦読みと縦書き用フォント表示をサポートする。
- 読書進捗 (現在のページ番号 + 最終アクセス日時) を 書籍×ユーザー単位で保存し、再開時はその位置から表示する。
- 進捗はリーダーを操作するたびにデバウンス付きで自動保存される。
- 書籍詳細画面では「現在のページ / 全ページ数」を可視化する。
- 書籍ごとのリーダー設定 (見開き、LTR/RTL など) も同じ単位で保存する。

## Mobile Application

### Android

OPDSを前提にした電子書籍リーダーアプリ。

#### 機能

- OPDSサーバーの追加・編集・登録
  - URLエントリポイントの指定
  - Basic認証のサポート
  - SSLの対応および自己署名証明書のサポート
- OPDSカタログの参照
  - カタログの階層構造の表示
  - 書籍のメタデータの表示 (タイトル、著者、出版社、出版日、タグなど)
  - 書籍のメタデータによるソート
  - 書籍の表紙画像の表示
  - リスト表示とグリッド表示の切り替え
  - サムネイルのキャッシュ
  - サーバーサイドの読書進捗の表示
    - 進捗同期は別途考える
- リーダー機能
  - EPUB、PDF、CBZの表示をサポートする
  - ディレクトリ直下の画像ファイルの場合は、CBZと同様にページ送りで表示する
  - ページ送りの操作は、タップやスワイプなどで行えるようにする
  - 左から右に読むのか右から左に読むのかを選択できる様にする
  - EPUBの場合は、目次の表示や、フォントサイズの変更、文字スペースや行間の変更、背景色の変更などの機能もサポートする
    - 将来的にはフォントの変更もサポートする
    - 縦書きとルビの表示もサポートする
  - ページジャンプのためのスクロールバーを下部に配置
  - ページ中央タップでメニューとスクロールバー表示をトグルできるようにする
  - スクロールバーを操作する時は対応するページのサムネイルを表示するようにする
  - パフォーマンスのためにページのプリロードをサポートする
  - サーバーサイドで余計なI/Oを発生させないように、ページのキャッシュ機能をサポートする
    - キャッシュキーはOPDSの書籍IDとページ番号の組み合わせで生成する
  - CBZ、PDFの場合は見開き表示のために2ページ同時表示をサポートする。
  - オフセットがズレている場合を想定し、見開き表示モードでも1ページずつページ送りができる機能も追加する。
  - タップエリアのカスタマイズ機能をサポートする
    - 通常のページ送り、戻り
    - 単ページ強制のページ送り・戻り
    - 連続ページ送り・戻り
  - EPUB, PDFの目次表示とページジャンプをサポートする
  - スマートフォン、タブレットなどの画面サイズに応じたレイアウトの最適化
  - ノッチや画面の切り欠きに対応するレイアウトの最適化
